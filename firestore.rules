rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // School Attendance System Security Rules
    // Allow read and write for the school administrative application
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
